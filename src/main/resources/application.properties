spring.application.name=AirplaneDemo

# MongoDB properties
spring.data.mongodb.host=${MONGODB_HOST}
spring.data.mongodb.port=${MONGODB_PORT}
spring.data.mongodb.database=${MONGODB_DATABASE}

schedule.rate=1000
